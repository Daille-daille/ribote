name: Ribote
type: profile
description: 'Drupal minimal distribution'
core_version_requirement: ^10.0 || ^11.0

distribution:
  name: Ribote
  install:
    theme: claro

install:
  - drupal:block
  - drupal:node

themes:
  - olivero
  - claro
